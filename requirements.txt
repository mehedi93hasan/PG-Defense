# PG-Def reproduction package — Python dependencies.
# Versions below are the ones the package was validated against; the lower
# bounds are conservative. Install with:
#     pip install -r requirements.txt
#
# PyTorch and the Adversarial Robustness Toolbox are only required for the
# adversarial-evaluation scripts (03, 07). Feature extraction (01) and clean /
# resource / cross-domain / ablation evaluation (02, 04, 05, 06) run without
# them, so on a CPU-only or constrained host you may install the core set
# first and add the adversarial extras later.

# ---- core ----
numpy>=1.24
pandas>=1.5
scikit-learn>=1.2
scipy>=1.10
xgboost>=1.7
imbalanced-learn>=0.10
pyyaml>=6.0

# ---- PCAP feature extraction (Tier 1/2) ----
dpkt>=1.9.8
mmh3>=3.0

# ---- adversarial evaluation (scripts 03, 07) ----
torch>=2.0
adversarial-robustness-toolbox>=1.17
